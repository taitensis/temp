---
import '@/styles/global.css';
import Header from '@/components/Header.astro'; // 👈 Nouveau composant
import Footer from '@/components/Footer.astro'; // 👈 Nouveau composant

interface Props {
    title: string;
    description?: string | null;
    // La langue doit être passée pour les composants Header/Footer i18n
    lang?: 'fr' | 'en'; 
}

// Assurez-vous de capturer la prop 'lang'
const { title, description = "Les recettes que j’aime !", lang = 'fr'} = Astro.props; 
---

<!DOCTYPE html>
<html lang={lang}>
    <head>
        <meta http-equiv="Content-Type" content="text/html;charset=UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content={description}>
        <title>{title} | Mes recettes préférées</title>
        <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    </head>

    <body class="min-h-screen bg-background font-sans antialiased flex flex-col">
        
        <Header lang={lang} />

        <main class="container mx-auto px-4 py-8 flex-grow">
            <slot />
        </main>

        <Footer lang={lang} />
    </body>
</html>